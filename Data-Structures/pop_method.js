// This function removes the element at the last index of an array.

//This function  is takes in no argument.

// Example

let arr = ['make', 'me', 'code', 'better', 'odd'];

arr.pop();

console.log(arr);