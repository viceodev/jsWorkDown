// The slice method is used to copy or extract a new array from an existing array following conditions passed to it without mutating the existing array.

let arr = ['odd', 'odd', 'make', 'me', 'code', 'better'];

let newArray =  arr.slice(2,6);

console.log(newArray, arr);